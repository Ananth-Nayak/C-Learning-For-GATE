#include <stdio.h>

int main(){
  int length=10, breadth=5;
  int length2,breadth2;
  printf("Enter the length and width of rectangle: ");
  scanf("%d %d",&length2,&breadth2);
  printf("area of reactangle1: %d\n",(length*breadth));
  printf("area of reactangle1: %d\n",(length2*breadth2));
  
  return 0;
}